spring:
  application:
    name: gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: user
          uri: lb://app-resources/
          predicates:
            - Path=/user/**
    consul:
      host: localhost
      port: 8500

server:
  port: 8000
